#!/usr/bin/env python3
def no_c(my_string):
    string = ""
    for i in my_string:
        if i != 'c':
            string = string + i
        elif i != 'C':
            string = string + i
    return string
